import type { AppManifest } from "@/os/types";

const manifest: AppManifest = {
  id: "marketplace",
  name: "Marketplace",
  icon: "Store",
  defaultWidth: 980,
  defaultHeight: 680,
  builtin: true,
};

export default manifest;

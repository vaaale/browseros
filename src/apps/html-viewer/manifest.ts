import type { AppManifest } from "@/os/types";

const manifest: AppManifest = {
  id: "html-viewer",
  name: "HTML Preview",
  icon: "Code2",
  defaultWidth: 900,
  defaultHeight: 640,
  builtin: true,
  hidden: true,
};

export default manifest;

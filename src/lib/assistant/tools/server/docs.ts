// Retired: docs_* tools were folded into the generic file_* tools (./files.ts).
// Docs are now readable/writable via the /Docs VFS mount (see
// @/os/fs/docs-fs.ts and @/lib/specs/spec-mount.ts) — writes require an active
// feature branch, same as any other BOS source change.
export {};

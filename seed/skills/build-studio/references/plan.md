Step: plan. Read the spec plus `/Templates/commands/plan.md` and `/Templates/plan-template.md` with `file_read`. If a `design` step ran (`references/design.md`), fold Architect's classification/Context-Container-Component/ADRs into this document — don't leave that analysis stranded in chat history. Write `/Specs/<store>/<id>/plan.md` with `file_write`: technical context; a Constitution Check against `/Specs/bos-system-specs/.specify/memory/constitution.md`; concrete project structure (real file paths); and design notes. Add research.md / data-model.md / contracts/ only when warranted.

You write plan.md (and those optional companions). Nothing else — no service.json, no index.js, no app/config files. Those real paths in "concrete project structure" are a plan of what will exist once `implement` delegates the work; they are not something to create yet.
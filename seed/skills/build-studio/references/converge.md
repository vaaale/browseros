Step: converge. Compare the implemented code against spec/plan/tasks (delegate investigation to the Developer if needed — `dev_delegate` for a BOS-source/built-in-app target, `agent_delegate` with `contentOnly:true` for a marketplace app, matching whichever target you used in `implement`). Append any remaining work to tasks.md, and record divergences between code and spec in bos-system-specs/discrepancies.md.
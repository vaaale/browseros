Step: converge. Compare the implemented code against spec/plan/tasks — read them with `file_read` for `bos-core`/`builtin-app`/`n/a` or `app_spec_read` for `marketplace-item` (delegate investigation to the Developer if needed — `dev_delegate` for a BOS-source/built-in-app target, `agent_delegate` with `contentOnly:true` for a marketplace app, matching whichever target you used in `implement`). Append any remaining work to tasks.md (`file_edit`/`app_spec_edit`, matching the target), and record divergences between code and spec in `/Specs/bos-system-specs/discrepancies.md` via `file_edit` — that file is genuinely system-store-only regardless of the drifted feature's own target.
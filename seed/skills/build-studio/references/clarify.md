Step: clarify. Read spec.md — `file_read` on `/Specs/<store>/<id>/spec.md` for `bos-core`/`builtin-app`/`n/a`, `app_spec_read` on `item-<id>/spec.md` for `marketplace-item` (check the App Target field if you don't already know it). Find ambiguities and [NEEDS CLARIFICATION] markers; ask the user concrete questions. Then append a '## Clarifications' section containing a '### Session <date>' list of Q→A, and update the affected requirements — `file_edit` or `app_spec_edit`, matching whichever you used to read it.

While reading, also scrutinize any Assumptions/Dependencies claim that concludes BOS source needs to change because of a technical limitation (e.g. "Next.js can't do X, therefore src/middleware.ts must change"). Check it against `references/target-marketplace-item.md` before accepting it — a protocol handler, arbitrary HTTP verbs, or a continuous background process is usually a `marketplace-item`'s service facet (its own worker-thread process, its own port, outside Next.js), not a BOS-source requirement. An unexamined false assumption here becomes a fact `plan` and `implement` will build on.
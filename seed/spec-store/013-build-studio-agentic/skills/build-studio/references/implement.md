Step: implement. Ensure spec.md, plan.md and tasks.md exist. Call delegate_to_developer with a complete task: the feature path (specs/<id>), a summary of the spec and plan, the tasks to execute, and acceptance criteria; instruct the Developer to edit files only within its provisioned worktree (do NOT create or switch branches — the Supervisor owns that), run typecheck/lint, and update docs. You never write code yourself — relay the Developer's result and reflect updated status.
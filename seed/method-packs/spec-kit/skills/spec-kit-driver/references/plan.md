Step: plan. Read `/Methods/spec-kit/templates/commands/plan.md` and `/Methods/spec-kit/templates/plan-template.md` with `file_read` (always — the active method's template mount is read-only regardless of target). Read the spec (and `design.md` if a `design` step ran) with `file_read` for `bos-core`/`builtin-app`/`n/a`, or `app_spec_read` for `marketplace-item`. If a `design` step ran, fold Architect's classification/Context-Container-Component/ADRs into this document — don't leave that analysis stranded in chat history. Write `plan.md` — `/Specs/<store>/<id>/plan.md` via `file_write` for `bos-core`/`builtin-app`/`n/a`, or `item-<id>/plan.md` via `app_spec_write` for `marketplace-item`: technical context; a Constitution Check against `/Specs/bos-system-specs/.specify/memory/constitution.md`; concrete project structure (real file paths); and design notes. Add research.md / data-model.md / contracts/ only when warranted (same tool-per-target split).

You write plan.md (and those optional companions). Nothing else — no service.json, no index.js, no app/config files. Those real paths in "concrete project structure" are a plan of what will exist once `implement` delegates the work; they are not something to create yet.
Step: analyze. Read spec + plan + tasks — `file_read` for `bos-core`/`builtin-app`/`n/a`, `app_spec_read` for `marketplace-item` — and report cross-artifact inconsistencies and coverage gaps (e.g. requirements with no task, tasks with no requirement, constitution violations). Report only — do not edit artifacts unless asked; if you do, use `file_edit`/`app_spec_edit` matching whichever you used to read them.